# Plan — remaining work (post-implementation)

## Code: complete on `test`. No further changes required for the feature itself.

## Verification (not yet run — do before relying on it)
1. **Split-reader unit check** — fake file list whose basenames match a few `PNG` rows; assert
   each lands in the bucket matching its `fold2` cell and an unlisted file is dropped; cross-check
   bucket counts against CSV column tallies.
2. **CLI smoke (no train)** — `python run.py --train -rev v4.4 --folds_csv <csv> …`; confirm base
   resolves to `regnety_160` (validates ordering) and fold auto = `fold1`; abort before the heavy
   loop. Spot-check v1.4/v2.4/v3.4/v5.4.
3. **Regression** — `--folds N` (random CV) and `--best` unchanged when `--folds_csv` absent.
4. **Lint/tests** — `ruff`, `pytest`.

## Then (maintainer, full dataset + GPU — out of repo scope)
- Stage `licensed_crossval_folds_CUT.csv` and point `--folds_csv`/`[TRAIN] folds_csv` at it.
- Run the 5 finetunings (3 epochs, same as before).
- Compare accuracy on the test split vs. the old models on the same eval data (minus 318); the
  issue notes 25/229 top-1 labels changed in small_samples.
- If parity holds → confirm new dataset as canonical release (closes the
  ufal/atrium-project#21 dependency). If not → fall back to seed-mapped splits minus 318.

## Minor observations (optional, not blockers)
- `REVISION_BEST_MODELS` still points at v*.3; `--best` ensembles the old models until the v*.4
  set is validated and the keys are swapped (already noted in a code comment).
- No `MODEL_STATIC` entries for v*.4 — only matters if memory-profiling/ensemble metadata is run
  against the new revisions later.
