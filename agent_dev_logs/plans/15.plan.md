# Issue #15 — Plan (post eval-feature integration)

## Context
The training fold path was merged on `test` (992c5df). This round adds the evaluation fold path so
the acceptance test is runnable end-to-end, and fixes a pandas-3.0 NA crash found in verification.

## Changes (done; in working tree, delivered as files, NOT committed)
1. **run.py — eval fold-subset**: after `collect_images` on the EVAL dir, when `--folds_csv` is set,
   reuse `split_data_from_folds(..., safe_check=False)` and keep only the `test` bucket. Downstream
   eval (run.py:~569) unchanged; the printed count reflects the fold test size.
2. **run.py — `resolve_fold_column(revision, explicit)` helper**: explicit `--fold_column` wins,
   else `REVISION_BEST_FOLDS[revision]`, else a clear `ValueError`. Used by both train and eval.
3. **classifier.py — NA-safe split**: `if fold_value is None or pd.isna(fold_value): exclude`;
   `str(fold_value).strip().lower()`. Handles blank/removed cells without crashing.
4. **README.md**: documents `--eval --folds_csv` and the single-model (not `--best`) constraint.
5. **tests/test_folds_split.py** (new, 18 tests): routing, −318/absent-page drop, NA/whitespace/
   case, error cases, the eval test-subset call, registry ordering (v4.4→regnety_160), the helper,
   and a `slow` real-CSV sanity check via `FOLDS_CSV`.

## Critical files
- run.py (eval filter ~:313, helper after imports, train branch ~:369)
- classifier.py (`split_data_from_folds` value handling)
- README.md (retraining/eval section ~:1565)
- tests/test_folds_split.py (new)
- No change needed in model_registry.py (v*.4 + `REVISION_BEST_FOLDS` already present).

## Verification
- `ruff check classifier.py run.py tests/test_folds_split.py`
- `pytest tests/test_folds_split.py` (and `tests/test_classifier.py -m "not slow"`)
- `FOLDS_CSV=<path> pytest tests/test_folds_split.py::test_real_folds_csv_counts_match`
- `python run.py --help` (flags registered); spot real-data split counts per fold.

## Remaining / out of scope
- Landing on `test` (the classifier.py NA fix touches the merged training path) — only on request;
  nothing is ever committed/pushed to ufal repos from here.
- The 5 actual finetunings + accuracy comparison (full licensed dataset + GPU; maintainer).
- `--best` ensemble eval with folds (ambiguous across differing per-model folds).
