# Issue #15 — Digest (implemented on `test` @ 992c5df)

## Goal
Retrain the 5 best models on the **new dataset (N−318 pages)** and confirm the new release as
canonical: each refinetuned model must reproduce its original accuracy on the **same eval data
(minus the 318 removed pages)**.

## Core requirement
Reproduce the **exact original train/dev/test split, minus removed pages** — impossible with the
stochastic `split_data_80_10_10()` (seeded `seed + i`). The fix: read the split from
`licensed_crossval_folds_CUT.csv` (`PNG` column = exact filename; `fold1..fold5` cells =
`train`/`dev`/`test`; 318 rows already removed).

## Per-model mapping (rule: splitN ↔ foldN ↔ seed = 420 + (N−1))
| Revision | Alias  | Base model            | Fold col | Seed |
|----------|--------|-----------------------|----------|------|
| v1.4     | EffNet | tf_efficientnetv2_m   | fold1    | 420  |
| v2.4     | Vb-2   | vit-base-patch16-224  | fold5    | 424  |
| v3.4     | Vb-3   | vit-base-patch16-384  | fold2    | 421  |
| v4.4     | RegN   | regnety_160           | fold1    | 420  |
| v5.4     | Vl-3   | vit-large-patch16-384 | fold2    | 421  |

## Implementation status — DONE
| Change                                                                        | File              | Status |
|-------------------------------------------------------------------------------|-------------------|--------|
| `split_data_from_folds()` + `import pandas`                                   | classifier.py     | ✅      |
| `v1.4`–`v5.4` entries (ordered before `vN.` prefixes) + `REVISION_BEST_FOLDS` | model_registry.py | ✅      |
| `--folds_csv` / `--fold_column`, auto-resolve, branch                         | run.py            | ✅      |
| `[TRAIN] folds_csv` key                                                       | setup/config.txt  | ✅      |
| Retraining docs + mapping table                                               | README.md         | ✅      |
