# 📓 Digest — [\#49](https://github.com/ufal/atrium-page-classification/issues/49) `open`: HEALTHCHECK, SIGTERM, and a publishable api image

> _Condensed from the live issue (fetched via API — no local `issues/` export exists yet for #49)
> — 0 comments, opener only. Second issue in this repo to get a digest/plan pair (after #48);
> follows that pair's convention._
> _Thread: 2026-09-07 · K4TEL._

**Labels**: `development`, `integration` · Milestone: Q3 [WP8 / WP7: Advanced Transnational Access & Curriculum Drafting] · Parent: [atrium-project#55](https://github.com/ufal/atrium-project/issues/55)
· **Assignees**: none

**Status**: ✅ **Filed as a retroactive checklist for work already committed.** `f1df2c5` ("edits
for k8s and added healthcheck for service") landed on `test` at 18:11; #49 was opened 19 minutes
later, at 18:30, as the tracking issue for it. Every box in the checklist is unticked in the issue
body but verifiably done in the tree.

## 🎯 Goal
Give page-classification a publishable API image with a working `HEALTHCHECK`, `STOPSIGNAL`, and
graceful `SIGTERM` drain — the page-classification-specific slice of hub issue #55.

## 🧵 Thread trail
- **2026-09-07, K4TEL** — opened as a nine-item task list, no prose beyond the items themselves,
  sub-issue of atrium-project#55.

## 🔎 What's already known (verified against the tree and live CI, not from this thread)
- All nine checklist items are implemented in `f1df2c5`: new `api` Dockerfile stage (there was
  previously no publishable API image — the service was reachable only via a compose `entrypoint:`
  override on the batch image); `HEALTHCHECK`/`STOPSIGNAL`/`--timeout-graceful-shutdown`;
  `serve_lifecycle`/`attach_inflight_middleware`/`/ready` wired through the shared
  `atrium_service.py`; blocking torch inference moved off the event loop; the batch entrypoint's
  `sh -c` PID-1 signal-forwarding gap closed with a pure-exec `os.execv` entrypoint; the
  `api: *gpu` compose alias spelled out; `build-targets`/`probe-targets` set in `docker.yml`; and
  `tests/test_service_runtime_deps.py` updated to read the Dockerfile stage rather than a compose
  override it no longer has.
- `service/atrium_service.py` and `service/healthcheck.py` are byte-identical to the hub's
  canonical copies at `v1` (direct blob-hash comparison) — this repo is in parity, not drifted.
- **CI ran and passed for the real image.** Run `34150657998` (`test`, `f1df2c5`): `build / test`
  green; `build-and-push (api)` green — the `-api` image was built, published, and Trivy-scanned
  clean; `build-and-push (base)` green too.
- **One thing did not run.** `build / docker-build-smoke` — the job that actually `docker run`s the
  image, waits for `HEALTHCHECK` to report `healthy`, curls `/health`/`/ready`, and `docker stop`s
  it to assert a clean `SIGTERM` exit — is gated to pull-request events in the hub's
  `docker-tool.reusable.yml`, and this landed via a direct push to `test`. It shows `skipped`, not
  `success`. So the specific container-level behavior this issue exists to add has not yet been
  machine-verified on this repo's own image.

## ⏳ Open
- The container smoke has never executed against page-classification's `-api` image — needs a PR
  (to trigger it) or a manual `docker run`/`docker stop` check before this can be called verified,
  not just implemented.
- The nine checklist boxes in the issue body are unticked despite the work being done.
- Parent atrium-project#55's own acceptance criterion (ARÚP/ARÚB confirming on their Kubernetes) is
  untouched by this issue and stays open regardless of #49's fate.
- See [`../plans/49.plan.md`](../plans/49.plan.md) for the verify-then-close sequence.
