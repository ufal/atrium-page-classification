evaluate>=0.4.6
configparser>=7.2.0
matplotlib>=3.11.1
# The <2.5 ceiling is LOAD-BEARING: numpy 2.5.0+ declares `Requires-Python >=3.12`
# and every image and CI job here runs 3.11 (`FROM python:3.11-slim`). Raise the
# floor past 2.5 and pip has no candidate at all — the install aborts outright, so
# `Docker Build & Publish` dies at dependency install, before any test runs.
# Dependabot did exactly that twice (#36, #37): a bare `<3.0` still *permits* 2.5.x,
# and the bot reads only the range, never Requires-Python. Its companion guard is the
# `numpy >= 2.5.0` ignore in .github/dependabot.yml — lift the two together when the
# images move to 3.12, or the bump is back next Monday. 2.4.6 is the newest release
# with cp311 wheels; 2.4.0 is yanked, hence the floor. Nothing here needs 2.5 — the
# highest transitive floor is pandas' `numpy>=1.26.0`.
numpy>=2.5.1,<2.6
pandas>=3.0.5
Pillow>=12.3.0
scikit-learn>=1.9.0
torch==2.7.1
torchvision==0.22.1
transformers>=4.57.6,<5
timm>=1.0.28
tensorboard>=2.21.0
ultralytics>=8.4.115
