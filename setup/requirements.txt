numpy>=2.4.0,<3.0
pandas>=3.0.3
Pillow>=12.3.0
torch==2.7.1
torchvision==0.22.1
# transformers 5.x constructs models on the `meta` device, which crashes the
# timm builders for the RegNetY (v4.3) and EfficientNetV2 (v1.3) checkpoints
# used by --best; the <5 pin is the meta-device fix and must stay consistent
# across every requirements file (see Dockerfile note).
transformers>=4.57.6,<5
timm>=1.0.27