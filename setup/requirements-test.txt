# Fast-lane + API meta-contract test deps (issue #32).
# Heavy model deps (torch/transformers) are intentionally omitted — the API
# contract test importorskips them, running only where the full stack exists.
pytest>=9.1.1
pytest-cov
fastapi
httpx
python-multipart
openapi-spec-validator
matplotlib

